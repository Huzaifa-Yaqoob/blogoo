import { Response } from "express";
import { CustomRequest } from "middleware/authenticate";
import { Report } from "db/database";

export const addReport = (req: CustomRequest, res: Response) => {};
